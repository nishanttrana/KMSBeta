// Standalone module so the prototype builds on its own. When you drop the
// package into the real repo at services/featureforge/, delete this file —
// the code already uses the import path vecta-kms/services/featureforge.
module vecta-kms

go 1.26.0
