# Burn-down list for the no-literal-key-material conformance rule
# (scripts/conformance.sh rule 3d, docs/SECURITY/SECURE_DEFAULTS.md).
#
# These files derive a master key from a string in the repo (or hardcode one)
# when their *_MEK_B64 is unset: the same defect fixed for the secrets service
# in 1.2.0-beta. Each needs the same treatment: refuse to start without a
# real key, installers generate it, and a startup migration re-wraps data
# stored under the public key. Remove a file in the change that fixes it.
# The list only shrinks: new files fail, and a fixed file left here fails.
services/certs/main.go
services/certs/service.go
services/cloud/main.go
services/cloud/service.go
services/ekm/main.go
services/ekm/service.go
