[Unit]
Description=Vecta KMS Container (pqc)
After=docker.service network-online.target
Wants=network-online.target
PartOf=vecta-containers.target

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/vecta
EnvironmentFile=-/etc/vecta/vecta.env
ExecStart=/usr/bin/docker compose -f /opt/vecta/docker-compose.yml up -d pqc
ExecStop=/usr/bin/docker compose -f /opt/vecta/docker-compose.yml stop pqc
ExecReload=/usr/bin/docker compose -f /opt/vecta/docker-compose.yml up -d pqc
TimeoutStartSec=300
TimeoutStopSec=120

[Install]
WantedBy=vecta-containers.target