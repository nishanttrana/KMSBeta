[Unit]
Description=Vecta KMS Container (valkey)
After=docker.service network-online.target
Wants=network-online.target
PartOf=vecta-containers.target

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/vecta
EnvironmentFile=-/etc/vecta/vecta.env
ExecStart=/usr/bin/docker compose -f /opt/vecta/docker-compose.yml up -d valkey
ExecStop=/usr/bin/docker compose -f /opt/vecta/docker-compose.yml stop valkey
ExecReload=/usr/bin/docker compose -f /opt/vecta/docker-compose.yml up -d valkey
TimeoutStartSec=300
TimeoutStopSec=120

[Install]
WantedBy=vecta-containers.target